steps:
- emerge -b ={{.PackageCategory}}/{{.PackageName}}-{{.PackageVersion}}
requires:
- category: "{{.LayerCategory}}"
  version:  "{{.LayerVersion}}"
  name:     "{{.LayerName}}"
- category: "{{.LayerCategory}}"
  version:  "{{.LayerVersion}}"
  name:     "sabayon-sources"
{{ if .Binhost }}
includes:
- /usr/portage/packages/.*
{{end}}
